version: '3.3'
services:
  bugsnag:
    build: ../bugsnag
    ports:
      - "80"
  aspnetcore:
    build: aspnetcore
    environment:
      - MAZE_API_KEY
      - MAZE_ENDPOINT
    ports:
      - "80"
networks:
  default:
    driver: transparent
